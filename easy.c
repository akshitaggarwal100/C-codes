#include <stdio.h>

int main()
{
    // char name[] = "12AB, 95 1A";
    // int len = sizeof(name) / sizeof(char) - 1;

    // int sum = 0;
    // for (int i = 0; i < len; i++)
    // {
    //     if (name[i] > 47 && name[i] < 58)
    //     {
    //         sum += name[i] % 48;
    //     }
    // }
    // printf("%d\n", sum);

    int n = 23;
    char a = 20;
    printf("%c", n + a);

    return 0;
}