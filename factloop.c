#include <stdio.h>

void main()
{
    int num;

    printf("Enter Number: ");
    scanf("%d", &num);

    for (int i = 0; i < num; i++)
    {
        for (int j = 0; j < ; j++)
        {
            
        }
        
    }
    
    // int a = 0, b = 1;

    // for (int i = 0; i < num - 2; i++)
    // {
    //     temp = a + b;
    //     a = b;
    //     b = temp;
    // }

    // printf("%d", temp);
}