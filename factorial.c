#include <stdio.h>

int main()
{
    // int number, output = 1;
    // printf("Enter a number: ");
    // scanf("%d", &number);

    // if (number == 0)
    // {
    //     printf("Factorial of %d is %d", number, output);
    //     return 0;
    // }

    // for (int i = number; i > 0; i--)
    // {
    //     output *= i;
    // }
    // printf("Factorial of %d is %d", number, output);
    int l = 3, b = 2;

    int p = 2*(l+b);

    printf("%d", p);
    
    return 0;
}